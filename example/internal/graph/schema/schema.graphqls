# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type User {
  id: ID!
  name: String!
  email: String!
}

type Query {
  users: [User!]!
  user(id: ID!): User
}

input NewUser {
  name: String!
}

type Mutation {
  createUser(input: NewUser!): User!
}
